package UD08._05_preguntas;

public class respuestas {
    /*
     * a) Se dice que instanciamos una clase cuando creamos objetos de dicha clase. --> Correcta
     * b) Se dice que instanciamos una clase cuando creamos una subclase de dicha clase.
     *
     * c) Si una clase es abstracta no se puede instanciar. --> Correcta
     * d) Si una clase es abstracta no se puede heredar de ella.
     *
     * e) Una clase abstracta tiene que tener métodos abstractos.
     * f) Una clase puede ser abstracta y no tener métodos abstractos. --> Correcta
     *
     * g) Si una clase tiene métodos abstractos tiene que ser abstracta. --> Correcta
     * h) Una clase puede tener métodos abstractos y no ser abstracta.
     *
     * i) Si una clase es abstracta sus subclases no pueden ser abstractas.
     * j) Una clase abstracta puede tener subclases que también sean abstractas. --> Correcta
     *
     * k) Si un método es abstracto en una clase, tiene que ser no abstracto en la subclase, o bien, la subclase tiene que ser también abstracta. --> Correcta
     * l) Si un método es abstracto en una clase, no puede ser abstracto en las subclases.
     *
     * m) Si un método se define final se tiene que reescribir en las subclases.
     * n) Si un método se define final no se puede reescribir en las subclases. --> Correcta
     *
     * o) Una clase puede tener un método final y no ser una clase final. --> Correcta
     * p) Si una clase tiene un método final tiene que ser una clase final.
     *
     * q) Si una clase se define final no se pueden definir subclases de ella. --> Correcta
     * r) Si una clase se define final no se puede instanciar.
     *
     * s) Un método definido final y abstract resultaría inútil, puesto que nunca se podría implementar en las subclases. --> Correcta
     * t) Un método definido final y abstract podría resultar útil.
     *
     * u) Una clase definida final y abstract resultaría inútil, puesto que no se podría instanciar ni heredar de ella. --> Correcta
     * v) Una clase definida final y abstract podría resultar útil.
    */
}
